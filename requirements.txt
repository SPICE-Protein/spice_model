# SPICE 依赖（版本钉死：与保存 checkpoint 的环境一致 — TF 2.21 + Keras 3.15）
# 国内建议：pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
tensorflow==2.21     # 钉死！Keras 3 跨版本优化器 checkpoint 布局/dtype 会变，续训 RestoreV2 报错
keras==3.15
datasets>=3.0
huggingface_hub>=0.23
pyarrow>=15.0
polars>=0.20
pyyaml>=6.0
matplotlib>=3.7      # 可视化（phase map / 论文图）
tensorboard==2.21    # tf.summary 曲线需要（缺失时代码自动降级为仅 CSV）
tqdm>=4.66           # 进度条（缺失时自动降级为逐步打印）
pytest>=7.0          # 开发：单元测试 (tests/)
